const success = "success";

const apiMsg = "Welcome to CS3219 Task B done by Marcus Tan. Use /api to use the Random Asian User API:)."

const welcomeMsg = "Test Cloud Run CD";

module.exports = {success, apiMsg, welcomeMsg};